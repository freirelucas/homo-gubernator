numpy>=1.26
scipy>=1.12
matplotlib>=3.8
pandas>=2.2
sympy>=1.12
networkx>=3.2
jupyter
ipykernel
nbformat
pysd>=3.14

# Cartografia opcional (instalar via pip ou conda conforme necessidade)
# geopandas>=0.14    # análise espacial vetorial
# cartopy>=0.22      # projeções cartográficas robustas (Cap. F4-02, F4-03)
# pygmt>=0.13        # mapas de qualidade publicação (use `conda install -c conda-forge pygmt`)
